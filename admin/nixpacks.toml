# Nixpacks configuration for Admin Panel
[phases.setup]
nixPkgs = ["nodejs-18_x"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "HOST=0.0.0.0 npx serve -s dist -l $PORT"

