{
  "name": "guardian-connect-admin",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "echo '🚀 Starting build process...' && tsc --version && echo '✅ TypeScript compiled' && vite build --mode production",
    "postbuild": "node -e \"const fs = require('fs'); const dist = './dist'; if (!fs.existsSync(dist)) { console.error('❌ ERROR: dist folder not created!'); process.exit(1); } if (!fs.existsSync(dist + '/index.html')) { console.error('❌ ERROR: index.html not found in dist!'); process.exit(1); } console.log('✅ Build verification passed');\"",
    "preview": "vite preview",
    "start": "npx http-server dist -p $PORT -a 0.0.0.0 --cors",
    "lint": "eslint . --ext ts,tsx --report-unused-disable-directives --max-warnings 0"
  },
  "dependencies": {
    "@tanstack/react-query": "^5.12.2",
    "axios": "^1.6.2",
    "http-server": "^14.1.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.20.1"
  },
  "devDependencies": {
    "@types/react": "^18.2.43",
    "@types/react-dom": "^18.2.17",
    "@typescript-eslint/eslint-plugin": "^6.14.0",
    "@typescript-eslint/parser": "^6.14.0",
    "@vitejs/plugin-react": "^4.2.1",
    "eslint": "^8.55.0",
    "eslint-plugin-react-hooks": "^4.6.0",
    "eslint-plugin-react-refresh": "^0.4.5",
    "typescript": "^5.2.2",
    "vite": "^7.2.2"
  }
}





